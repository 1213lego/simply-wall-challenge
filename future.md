
cases to see in the future
data ingestions abstraction to support multipless sources and in real time.
splits and dividents
In the future in portfolio, currency. As the user will want to see the portfolio value in their favourite currency and it can have transactions of different currencies.
users for portfolio and authentication
evaluate target clients, for UIs graphql
a different database for historical_prices, as this table can grow really fast. another option will be denormalize historical_prices for faster lookup of prices and inserts when the tables rows are massive. Definitly depth dive more into this topic
async bulk upload(csv) with  a post transaction review to accept or reject transactions
dependency injection